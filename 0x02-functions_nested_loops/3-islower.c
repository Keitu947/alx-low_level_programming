#include "main.h"

/**
 * _islower - checks for lower case letter
 * @c : character to check the case
 * Return:0 or 1
 */
int _islower(int c)
{
	if (islower(c))
  {
    return 0;
  }
  
  return 1;
}
